# Postgres connection string (Neon in production). The only required variable.
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Optional: turns on "look up this card's benefits online". Get a key at
# https://console.anthropic.com. Unset = the lookup buttons explain how to enable it.
ANTHROPIC_API_KEY=""
